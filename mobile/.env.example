# Normalement inutile : l'application déduit l'adresse du backend du serveur
# Expo auquel le téléphone est déjà connecté.
#
# À renseigner seulement si le backend tourne sur une autre machine que le
# serveur Expo, ou sur un autre port.
# EXPO_PUBLIC_API_URL=http://192.168.1.20:3000
